# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
    strict-inference: true
  errors:
    # Трактуем как ошибки то, что чаще всего приводит к багам.
    dead_code: error
    unused_import: error
    unused_local_variable: error
    missing_return: error
    always_declare_return_types: error
    invalid_assignment: error
    implicit_dynamic_parameter: error
    implicit_dynamic_variable: error
    implicit_dynamic_type: error

linter:
  rules:
    # Строгая декларация типов/читаемость
    always_declare_return_types: true
    always_put_required_named_parameters_first: true
    always_specify_types: false
    prefer_final_locals: true
    prefer_final_in_for_each: true
    unnecessary_lambdas: true
    avoid_redundant_argument_values: true
    unnecessary_parenthesis: true

    # Стиль кода / кавычки / импорты
    prefer_single_quotes: true
    directives_ordering: true
    sort_child_properties_last: true
    specify_non_nullable_return_types: true

    # Безопасность/ошибки
    avoid_print: true
    use_build_context_synchronously: true
    cancel_subscriptions: true
    close_sinks: true
    no_leading_underscores_for_local_identifiers: true

    # Коллекции/итерации
    avoid_function_literals_in_foreach_calls: true
    prefer_foreach: false
    avoid_unnecessary_containers: true

    # Константы/конструкторы
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    unnecessary_const: true
    unnecessary_new: true

    # Нул-безопасность и проверка условий
    prefer_is_empty: true
    prefer_null_aware_method_calls: true
    null_check_on_nullable_type_parameter: true

# Доп. справка
# https://dart.dev/guides/language/analysis-options
